# Acme Customer Portal — pinned dependencies as currently deployed (v2.4.0)
# Cross-reference with dependency-advisories.md during Lab 7.
# Do NOT `pip install` this file — it pins deliberately outdated versions for the exercise.

requests==2.25.1
pyyaml==5.3.1
jinja2==2.11.3
markupsafe==2.0.1
pillow==8.1.0
urllib3==1.26.4
python-dateutil==2.8.2
pytest==7.4.0
