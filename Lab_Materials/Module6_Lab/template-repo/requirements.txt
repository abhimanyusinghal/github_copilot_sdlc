# Runtime + test dependencies for the Acme Customer Portal slice.
# Some versions are intentionally outdated so Dependabot raises real alerts in Lab 7.
# The unit suite needs pytest; Lab 7's config test uses PyYAML. The remaining
# packages represent the wider portal dependency snapshot used for advisory triage.
pytest==7.4.0
requests==2.25.1
jinja2==2.11.3
markupsafe==2.0.1
pyyaml==5.3.1
urllib3==1.26.4
